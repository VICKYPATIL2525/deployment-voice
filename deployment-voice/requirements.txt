# =============================================================================
# Mindspace Deployment — API Server Requirements
# Minimal dependencies for running the FastAPI inference servers:
#   - api_text_to_sentiment.py  (LightGBM, 43 text features, port 9000)
#   - api_voice_to_sentiment.py (XGBoost, 1351 acoustic features, port 9100)
# Versions pinned to what is installed in myenv (verified via pip freeze).
# =============================================================================

# ── API framework ─────────────────────────────────────────────────────────────
fastapi==0.134.0
uvicorn[standard]==0.41.0
pydantic==2.12.5
python-multipart==0.0.22

# ── ML inference (load and run the trained models) ───────────────────────────
numpy==2.4.2
pandas==2.3.3
scikit-learn==1.8.0
lightgbm==4.6.0          # Text API — LightGBM classifier
xgboost==3.2.0            # Voice API — XGBoost classifier
joblib==1.5.3

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.2.1
